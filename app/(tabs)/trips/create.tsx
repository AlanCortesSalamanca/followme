import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function CreateTripScreen() {
  return <ScreenPlaceholder title="Crear viaje" description="Formulario MVP: titulo, descripcion y codigo de invitacion." />;
}
